import { Paper, styled } from '@mui/material';

export const NewsCard = styled(Paper)({
  display: 'flex',
  flexDirection: 'column',
  gap: '2rem',
  height: '100%',
  padding: '2rem',
  textAlign: 'center',
});
